<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="./style.css">
</head>
<body>
   
        <% messages.forEach((message)=>{ %>
            <ul>
            <li><%= message.text %></li>
            <li><%= message.user %></li>
            <li><%= message.added %></li>
            <button>
                <a href=<%=`messages?id=${message.id}`%> >abrir</a>
            </button>
            </ul>
        <% }) %> 
  
  
</body>
</html>